version: '3.3'

services:
  oloscreen_node:
    container_name: oloscreen_node
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile-build
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 3000:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
